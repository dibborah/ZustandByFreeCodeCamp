npm i --save-dev vitest @testing-library/jest-dom @testing-library/react jsdom

# This is a shell script file so the .sh extension
# Pound symbol is  used for writting comments in shell script files
# In this files commands are written which are used or to be excuted in the application

